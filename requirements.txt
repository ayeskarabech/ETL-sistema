pandas>=1.5.0
polars>=0.20.0
openpyxl>=3.0.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
jinja2>=3.1.0
# pyarrow: exigido por polars.DataFrame.to_pandas() e pelo ParquetHandler
pyarrow>=14.0.0
# fastexcel: engine usada por polars.read_excel() (leitura rapida de .xlsx)
fastexcel>=0.10.0
# duckdb: motor de JOIN/PROCV/agregacao para bases grandes (DuckDBEngine)
duckdb>=1.0.0
# supabase>=2.0.0  # descomente para ativar integracao com Supabase
