To run:

python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --limit-request-body 0


Then open http://localhost:8000 — it auto-redirects to the SPA.

The --limit-request-body 0 removes the body size limit for large file uploads.
